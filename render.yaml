services:
  - type: web
    name: entropic-writing-lab
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
    headers:
      - path: /*
        name: X-Frame-Options
        value: ALLOWALL
